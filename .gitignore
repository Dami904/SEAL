# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Node / pnpm
node_modules/
.next/
dist/
*.tsbuildinfo

# Local env files — never committed
.env
.env.local
*.local

# Generated backtest output and the derived (non-raw) per-symbol datasets —
# reproducible from committed raw sources (data/*_ohlcv_raw.json + scripts/),
# regenerated by CI, not carried as fixtures that can silently go stale.
reports/
data/r*_series.csv
data/sample_series.csv
